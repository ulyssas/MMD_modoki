Place custom toon ramp textures for runtime fallback presets here.

Suggested first file:
- `fallback_shadow_toon.bmp`

This folder is intended for preset-specific toon textures such as:
- forced fallback toon ramps for `full_shadow`
- fixed toon ramps for cel-look presets
