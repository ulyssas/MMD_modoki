# lut

LUT 関連ファイルの整理用フォルダです。

用途:

- 今後追加する同梱 LUT プリセットの実体ファイル置き場
- LUT パックや比較用サンプルの保管
- LUT 変換メモや命名ルールの整理

現状の built-in LUT は `src/mmd-manager.ts` 内で 3dl テキストを生成しているため、このフォルダからはまだ直接読み込んでいません。

補足:

- プロジェクト保存時の相対 LUT は引き続き `<project_dir>/luts/` に書き出す
- リポジトリ内の整理用フォルダとしては `lut/` を使う
