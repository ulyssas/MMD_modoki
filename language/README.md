# language

UI 多言語辞書を置くフォルダです。

- `ja.json`: 日本語 UI 辞書
- `en.json`: 英語 UI 辞書

`src/i18n.ts` はこのフォルダの JSON を読み込み、`i18next` に渡す薄いラッパーとして動作します。

運用ルール:

- 追加するキーは `ja.json` と `en.json` の両方へ入れる
- DOM 反映は `data-i18n*` 属性経由を優先する
- 実験的な文言はまず日本語側で固めてから英語へ展開する
